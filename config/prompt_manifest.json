{
  "schema": "prompt_manifest_v3",
  "prompt_strategy": "base_motion_fixed_prompt_v1",
  "anchor_backend": {
    "name": "image_embedding",
    "source": "semantic_anchors.motion_states.semantic_states"
  },
  "positive_parts": [
    "prompt_base",
    "prompt_motion",
    "prompt_stability"
  ],
  "negative_field": "prompt_negative",
  "notes": [
    "Positive prompts are fixed base, motion, and stability constraints.",
    "Visual anchors are used only for generation unit boundary planning."
  ]
}
