"""Decode stage namespace.

Decode owns image generation + sequence merge in the target architecture.
Phase 1 keeps the current implementation in legacy modules and introduces this
directory as the future single ownership boundary.
"""
